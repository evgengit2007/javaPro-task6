server:
  port: 8989

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/javaPro
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: none